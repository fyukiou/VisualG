Algoritmo "tabuada"

//algoritmo para calcular e exibir a tabuada do número informado//

Var

   i, N, produto : inteiro

Inicio

      escreva("deseja a tabuada para qual valor?: ")
      leia(N)
      
      para i de 1 ate 10 faca
      
      
        produto <- N * i
        escreval(N, "x", i, "=", produto)
        
      fimpara

Fimalgoritmo